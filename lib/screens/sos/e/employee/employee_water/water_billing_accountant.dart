import 'package:flutter/material.dart';

class WaterBillingAccountantScreen extends StatelessWidget {
  const WaterBillingAccountantScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('محاسب فواتير المياه')),
      body: const Center(child: Text('محتوى شاشة محاسب فواتير المياه')),
    );
  }
}
