import 'package:flutter/material.dart';

class ReportingOfficerWaterScreen extends StatelessWidget {
  const ReportingOfficerWaterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('مسؤول الإبلاغ عن المياه')),
      body: const Center(child: Text('محتوى شاشة مسؤول الإبلاغ عن المياه')),
    );
  }
}
