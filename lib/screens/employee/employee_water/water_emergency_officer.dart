import 'package:flutter/material.dart';

class WaterEmergencyOfficerScreen extends StatelessWidget {
  const WaterEmergencyOfficerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ضابط طوارئ المياه')),
      body: const Center(child: Text('محتوى شاشة ضابط طوارئ المياه')),
    );
  }
}
