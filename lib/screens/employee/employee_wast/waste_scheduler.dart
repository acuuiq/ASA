import 'package:flutter/material.dart';

class WasteSchedulerScreen extends StatelessWidget {
  const WasteSchedulerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Waste Scheduler Screen')));
  }
}
